ticker:
    interval: 5000
collectors: "file"
filters: "id"
handlers: "log"
collector:
  file:
    path: "resources/qframe.yml"
    reopen: true                  # If file is truncated it will be reopened
handler:
  log:
    inputs: "file"
